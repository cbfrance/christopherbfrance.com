<% page_articles.each_with_index do |article, i| %>
  <article>
    <h2><%= link_to article.title, article %></h2>
    <span class="timestamp"><%= article.date.strftime('%b %e %Y') %></span>
    <%= article.summary(250) %>
    <%#= article.body %>
  </article>
<% end %>